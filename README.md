--> Use the notebook in /prod_notebook "data_exploration" to access the best-performing model so far.

--> The "data_exploration_test_2" notebook in /test_notebook is a testing notebook using a larger set of category codes, with a slightly less performant model.

To guarantee reproducibility in production environments, the repository includes:

requirements.txt — minimal project dependencies

requirements-dev.txt — development and analysis tools

requirements-prod.txt — frozen dependency versions for prod
