# Core runtime dependencies
anthropic>=0.40            # Claude API + structured outputs
youtube-transcript-api>=1.0     # captions
feedparser>=6.0.11        # RSS parsing
httpx>=0.27              # channel-id resolution
PyYAML>=6.0              # config
pydantic>=2.6           # config + note models
python-slugify>=8.0      # safe filenames
tenacity>=8.2           # backoff/observability around network + LLM

# Whisper fallback (optional but installed in CI so captions-less videos still work)
yt-dlp>=2024.8.6         # audio download for the whisper fallback
faster-whisper>=1.0.3    # CPU-friendly transcription (CTranslate2)
